const Footer = ({ apiBase }) => (
    <footer className="mt-16 text-center text-xs text-gray-500 select-none">
        Using API base: <span className="font-mono">{apiBase}</span>
    </footer>
);

export default Footer;
